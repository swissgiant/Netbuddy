# Dell OS6 (N-Series / PowerConnect) — read-only.
# System-Info gegen ECHTEN Capture validiert (BLS-SW-51 / N2248PX-ON / 6.6.3.15);
# interfaces/lldp/mac sind doku-/research-abgeleitet und werden per Live-Probe bestätigt.
adapter_id: dell_os6
provenance: "Dell OS6 — sysinfo lab-validated (N2248PX-ON 6.6.3.15); interfaces/lldp/mac unvalidated"

backup_command: "show running-config"

capabilities:
  read_system_info:
    command: "show version"
    parser: "textfsm:dell_os6_show_version.textfsm"
    fields:
      vendor: { const: dell }
      model: model
      os_version: os_version
      serial_number: serial
      device_type: { const: switch }

  read_interfaces:
    command: "show interfaces status"
    parser: "textfsm:dell_os6_show_interfaces_status.textfsm"
    fields:
      name: port
      description: { from: description, via: [strip_or_none] }
      oper_status:
        from: link
        via:
          - lookup:
              table: { up: up, down: down }
              default: unknown
      speed_mbps: { from: speed, via: [leading_int] }

  read_lldp:
    command: "show lldp remote-device detail"
    parser: "textfsm:dell_os6_show_lldp_remote-device_detail.textfsm"
    fields:
      local_interface: local_interface
      remote_chassis_id: chassis_id
      remote_port_id: port_id
      remote_port_description: { from: port_desc, via: [strip_or_none] }
      remote_system_name: { from: system_name, via: [strip_or_none] }
      remote_system_description: { from: system_desc, via: [strip_or_none] }
      mgmt_address: { from: mgmt_address, via: [ip_or_none] }

  read_arp:
    command: "show arp"
    parser: "textfsm:dell_os6_show_arp.textfsm"
    drop_when_empty: [mac_address]
    fields:
      ip_address: ip_address
      mac_address: mac
      interface: { from: interface, via: [strip_or_none] }

  read_mac_table:
    command: "show mac address-table"
    parser: "textfsm:dell_os6_show_mac_address-table.textfsm"
    drop_when_empty: [interface]
    fields:
      mac_address: mac
      interface: { from: port, via: [strip_or_none] }
      vlan_id: { from: vlan, via: [int_or_none] }
      entry_type:
        from: type
        via:
          - enum_value: { values_of: MacEntryType, default: dynamic }
